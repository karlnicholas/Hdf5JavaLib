/**
 * HDF5 file metadata management.
 * <p>
 * The {@code org.hdf5javalib.file.metadata} package handles HDF5 file metadata,
 * particularly the superblock, which is the root metadata structure defining the
 * file’s layout and structure. It provides classes for parsing and writing top-level
 * file metadata.
 * </p>
 */
package org.hdf5javalib.hdffile.metadata;